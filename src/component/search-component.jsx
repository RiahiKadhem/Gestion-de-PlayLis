
import React from 'react'
import UlComponent from 'component/ul-component'

const SearchComponent = ({ master, onClick }) => (

    <UlComponent options={master} onClick={onClick} />

)
export default SearchComponent
